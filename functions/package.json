{
  "name": "functions",
  "description": "Cloud Functions for Kisan-Drishti",
  "scripts": {
    "build": "echo 'No build step'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.5.0"
  }
}
